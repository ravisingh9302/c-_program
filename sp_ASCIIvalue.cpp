#include <iostream>
using namespace std;

int main()
{
    char alpha;
    cout<<endl<< endl;
    cout << "Enter the character for which you want to know the ASCII value " << endl;
    cin >> alpha;
    cout << "the ASCII value of '" << alpha << "' is " << int(alpha) << endl;

    return 0;
}